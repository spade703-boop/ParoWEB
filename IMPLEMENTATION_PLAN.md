# 抽派生公开网站：项目背景与实施计划

> 文档状态：已确认需求，可直接用于新对话继续实施  
> 新项目目录：`C:\Users\Hasee\Desktop\paro_web`  
> 架构参考项目：`C:\Users\Hasee\Desktop\gemini_bot`  
> 编写日期：2026-09-18

## 1. 项目背景

现有 `gemini_bot` 是一个基于 NoneBot 的 QQ 机器人，其中包含“抽派生”功能。该功能可以从彰人和冬弥的派生池中随机组合两个派生，也支持固定一方、随机另一方；同时包含“做饭”和狐狸、兔子等特殊结果，并记录个人抽取统计。

计划将这个玩法迁移为一个任何获得网址的人都可以使用的公开响应式网站。经过讨论，最终不采用微信小程序，原因包括账号注册、类目、备案、合法域名和平台审核链路较长。首版采用独立网站，可以在手机和电脑浏览器中直接使用。

该网站应当是一个**独立项目**，不作为 `gemini_bot` 的子模块，不依赖 NoneBot，也不能要求机器人进程正在运行。现有 Bot 仅作为规则、数据结构、视觉和历史行为的参考来源。

## 2. 已确认的产品决策

以下内容已经确定，实施时不需要再次选择：

- 产品形态：独立的响应式网站，不开发微信小程序。
- 开放范围：面向公开互联网。
- 用户身份：无需注册和登录，以匿名浏览器 Cookie 识别用户。
- 记录期限：同一浏览器长期保留；更换设备、清除 Cookie 后无法找回。
- 首版功能：
  - 随机抽取；
  - 固定彰人、随机冬弥；
  - 固定冬弥、随机彰人；
  - 每次可抽 1、2 或 3 个组合；
  - 查看双方派生池；
  - 查看个人汇总和最近 50 个结果。
- 首版不包含：公开排行榜、账号注册、后台管理、QQ 与网页账号绑定、微信登录。
- 抽取规则：保留 Bot 当前做饭概率、特殊结果概率、特殊结果权重和同批次标签去重规则。
- 次数规则：网站取消 Bot 的“30 分钟最多 3 抽”产品限制，但保留接口级防刷限流。
- 数据关系：网站与 Bot 共用内容来源，但个人统计完全分开，网站数据不得写入 `paro_stats.json` 或 QQ 群排行。
- 运行方式：网站后端在现有阿里云 ECS 上以独立 Docker 容器运行，不挂载到 `mybot` 进程。
- 服务器地域：中国大陆，需要考虑 ICP 备案。
- 首版素材：继续使用现有派生头像，但正式公开前必须确认其公开使用权。

## 3. 与 Bot 项目的关系

### 3.1 可参考的现有实现

参考仓库根目录：

`C:\Users\Hasee\Desktop\gemini_bot`

重点参考文件如下：

| 参考位置 | 用途 |
| --- | --- |
| `nonebot_plugin_akito/features/random_paro/draw.py` | 抽取、定向匹配、一次 1–3 抽、特殊结果选择和同批次标签去重 |
| `nonebot_plugin_akito/features/random_paro/store.py` | 派生池与特殊结果配置的默认值、读取和标准化方式 |
| `nonebot_plugin_akito/features/random_paro/stats.py` | 做饭、特殊结果、双方命中和组合命中的统计口径 |
| `nonebot_plugin_akito/features/random_paro/assets.py` | 头像和狐狸/兔子素材的查找约定 |
| `nonebot_plugin_akito/features/random_paro/views.py` | 抽取结果和个人资料页的数据契约参考 |
| `nonebot_plugin_akito/features/random_paro/templates/` | 现有卡片的视觉层次和配色参考，不直接照搬服务端图片渲染 |
| `data/paro_pools.json` | 彰人和冬弥派生池 |
| `data/paro_config.json` | 做饭概率、特殊结果概率、权重、标签、文案和素材映射 |
| `data/images/paro_avatars/` | 派生头像与狐狸/兔子素材 |
| `tests/features/random_paro/` | 当前规则、统计和渲染行为的回归依据 |

### 3.2 必须保持的边界

- 不从新项目导入 `nonebot_plugin_akito`。
- 不依赖 Bot 的 `.env`、NoneBot 初始化、QQ 事件类型或命令注册。
- 不读写 `paro_stats.json`、`paro_egg_log.jsonl` 等机器人运行统计。
- 不直接操作生产 Bot 容器。
- 不在 `gemini_bot` 仓库中新增网站代码。
- 需要的规则应在新项目中重新整理成无框架依赖的领域模块；不是简单复制整个 `random_paro` 包。
- 初次迁移完成后，新网站有自己的测试和版本。以后 Bot 规则变化时，通过人工同步或内容发布流程更新，不能让两个项目形成隐式 Python 依赖。

## 4. 项目目标与验收标准

### 4.1 用户目标

访客打开网址后，无需注册即可：

1. 选择抽取 1、2 或 3 个派生组合；
2. 选择完全随机，或固定任意一方的某个派生；
3. 看到包含名称、头像、做饭或特殊结果提示的结果卡片；
4. 浏览彰人和冬弥的全部派生；
5. 查看自己的累计抽取、做饭/特殊结果统计、TOP 3 和最近 50 条结果；
6. 主动清除自己的全部网站记录。

### 4.2 技术验收

- 手机与桌面端均可正常使用，不出现横向溢出。
- 网站重启、容器重建后个人记录仍然存在。
- 同一浏览器能恢复原有匿名身份。
- 并发抽取不会丢记录、重复覆盖统计或破坏数据库。
- 一次抽取的结果、统计和最近记录在同一数据库事务中写入。
- Bot 数据和网站数据物理隔离。
- 派生池或配置文件异常时，不以空池覆盖当前可用内容。
- 缺失头像时使用文字占位，不影响抽取。
- 所有公开接口均有输入验证、请求限流和统一错误响应。
- 生产环境仅通过 HTTPS 对外开放。

## 5. 推荐技术架构

### 5.1 技术栈

- Python 3.11（如需与现有运维环境统一，也可保持 Python 3.10）。
- FastAPI：HTTP API 和静态页面入口。
- Uvicorn：ASGI 服务进程。
- `aiosqlite`：异步访问 SQLite。
- Pydantic：请求与响应模型验证。
- 原生 HTML、CSS、JavaScript：响应式单页前端。
- Pytest：单元测试和 API 集成测试。
- Docker：生产部署。
- Nginx：HTTPS、反向代理、安全头和静态缓存。

首版不引入 React/Vue、Node 构建链、Redis、PostgreSQL、Celery 或对象存储。只有在真实流量证明 SQLite 或单实例服务不足时再升级。

### 5.2 逻辑分层

```text
浏览器
  ├─ 抽取页 / 派生池 / 我的记录
  └─ 同源 JSON API
          │
          ▼
FastAPI Web 层
  ├─ 会话 Cookie
  ├─ 参数校验
  ├─ 限流与安全校验
  └─ 错误转换
          │
          ▼
应用服务层
  ├─ 创建抽取批次
  ├─ 记录结果和更新聚合
  ├─ 读取个人记录
  └─ 清除个人记录
          │
    ┌─────┴─────┐
    ▼           ▼
领域核心      SQLite 仓储
抽取与统计     网站独立数据
    │
    ▼
只读内容快照
派生池 / 特殊配置 / 头像
```

### 5.3 建议目录结构

```text
paro_web/
├─ app/
│  ├─ __init__.py
│  ├─ main.py                 # FastAPI 创建、路由挂载、生命周期
│  ├─ config.py               # 环境变量与路径配置
│  ├─ domain/
│  │  ├─ models.py            # 无 Web/数据库依赖的领域类型
│  │  ├─ draw.py              # 抽取与定向匹配
│  │  └─ statistics.py        # 统一统计口径
│  ├─ services/
│  │  ├─ catalog.py           # 内容快照与热重载
│  │  ├─ draw_service.py      # 抽取事务编排
│  │  └─ profile_service.py   # 个人统计与历史
│  ├─ repositories/
│  │  └─ sqlite.py            # 建表、迁移、事务和查询
│  ├─ web/
│  │  ├─ api.py               # JSON API
│  │  ├─ sessions.py          # 匿名会话 Cookie
│  │  ├─ rate_limit.py        # 防刷限制
│  │  └─ errors.py            # 统一错误格式
│  └─ static/
│     ├─ index.html
│     ├─ app.css
│     └─ app.js
├─ content/
│  ├─ paro_pools.json
│  ├─ paro_config.json
│  └─ images/paro_avatars/
├─ data/                      # 运行时 SQLite；不提交 Git
├─ tests/
├─ Dockerfile
├─ .dockerignore
├─ .gitignore
├─ .env.example
├─ pyproject.toml
├─ README.md
└─ IMPLEMENTATION_PLAN.md
```

## 6. 领域规则

### 6.1 抽取方式

- `random`：彰人和冬弥都从各自派生池随机选择。
- `akito`：固定指定彰人派生，冬弥随机。
- `toya`：固定指定冬弥派生，彰人随机。
- 每个请求的 `count` 只能是 1、2、3。
- 固定名称必须能在对应派生池中唯一匹配。
- 前端应直接提交派生池中的完整名称；后端仍保留精确匹配及兼容性的模糊匹配，但匹配到多个结果时必须返回可读错误，不能擅自选择。

### 6.2 概率规则

默认值以现有 `paro_config.json` 为准：

- 做饭概率：3%。
- 特殊结果总概率：8%。
- 特殊结果内部根据 `weight` 加权选择。
- 同一批多抽中，已占用的特殊标签不得再次使用；例如复合狐狸兔子结果会同时占用 `fox` 和 `rabbit` 标签。
- 如果所有可选特殊结果都因标签冲突被排除，该次回落为普通结果。
- 做饭判定优先于特殊结果：已命中普通做饭时不再命中特殊结果。

不要在前端执行随机逻辑。所有随机选择必须由服务端完成，前端只展示服务器返回的结果。

### 6.3 统计口径

- `draw_count` 按单个结果累计，而不是按 HTTP 请求次数累计；一次三抽增加 3。
- 普通结果：累计彰人派生命中、冬弥派生命中和组合命中。
- 普通做饭结果：仍是普通组合，因此同时累计双方、组合和做饭次数。
- 特殊结果：累计总抽数与对应特殊类型；不累计双方和组合命中。
- 配置中 `counts_as_cooking=true` 的特殊结果还要累计做饭次数。
- TOP 3 分为彰人、冬弥和组合三组；按次数降序，同次数按最近命中顺序或稳定名称顺序处理，整个项目必须统一。

## 7. 内容与素材管理

### 7.1 首版策略

新项目初始内容从 Bot 项目复制：

- `data/paro_pools.json` → `content/paro_pools.json`
- `data/paro_config.json` → `content/paro_config.json`
- `data/images/paro_avatars/` → `content/images/paro_avatars/`

复制后由新项目独立版本控制。生产网站不应直接从 Bot 仓库路径读取文件，这样可以避免 Bot 更新、回滚或目录权限影响网站。

### 7.2 内容校验

服务启动和热重载时必须校验：

- 两个派生池均为非空字符串数组；
- 同一个池中没有空项和重复项；
- 概率在 0 到 1 之间；
- 特殊结果 ID 唯一；
- 权重为有限正数；
- 标签为非空数组；
- 素材路径只能落在配置的素材根目录内；
- 每个派生可缺少头像，但要记录警告并使用占位图。

新文件校验失败时保留上一个有效内容快照，API 返回的目录不能突然变为空。

### 7.3 更新方式

首版不做后台管理。维护派生池时：

1. 在本地修改 JSON 或素材；
2. 运行内容校验测试；
3. 提交新项目仓库；
4. 部署新版本或替换只读内容卷；
5. 服务检测修改时间并原子加载新快照。

后续如果频繁维护，再增加受保护的后台，不在首版提前实现。

## 8. 匿名身份设计

### 8.1 会话 Cookie

- 首次访问由服务端生成至少 256 bit 的加密安全随机令牌。
- 浏览器 Cookie 仅保存原始随机令牌。
- 数据库只保存令牌的 SHA-256 摘要，避免数据库泄露后令牌被直接复用。
- Cookie 属性：`HttpOnly`、`Secure`、`SameSite=Lax`、`Path=/`。
- 建议有效期一年，每次正常访问可滚动续期。
- Cookie 名不要使用 `session` 等过于通用的名称，例如 `paro_visitor`。
- 不收集微信身份、QQ 号、昵称、手机号、邮箱或精确位置。

### 8.2 用户预期

隐私说明和“我的记录”页面必须明确：

- 记录与当前浏览器 Cookie 绑定；
- 清理浏览器数据、使用无痕模式或更换设备会失去记录；
- 当前版本没有账号恢复能力；
- 用户可以在页面内主动删除记录。

## 9. 数据库设计

使用独立 SQLite 文件，例如：

`data/paro_web.sqlite3`

建议启用：

- WAL 模式；
- 外键约束；
- `busy_timeout`；
- 每个请求一个短生命周期连接或受控连接池；
- 所有抽取写入都在显式事务中完成。

### 9.1 `visitors`

| 字段 | 含义 |
| --- | --- |
| `id` | 内部 UUID 或整数主键 |
| `token_hash` | Cookie 令牌摘要，唯一 |
| `created_at` | 首次访问时间 |
| `last_seen_at` | 最近访问时间 |

### 9.2 `draw_batches`

| 字段 | 含义 |
| --- | --- |
| `id` | 抽取批次 ID |
| `visitor_id` | 匿名访客 |
| `requested_count` | 请求抽数 |
| `fixed_side` | `none`、`akito` 或 `toya` |
| `fixed_name` | 被固定的派生名，可空 |
| `created_at` | 服务端 UTC 时间 |

### 9.3 `draw_results`

| 字段 | 含义 |
| --- | --- |
| `id` | 结果 ID |
| `batch_id` | 所属批次 |
| `position` | 批次内顺序 1–3 |
| `akito_name` | 当时抽到的彰人派生 |
| `toya_name` | 当时抽到的冬弥派生 |
| `is_cooking` | 是否普通做饭 |
| `special_type` | 特殊结果 ID，可空 |
| `counts_as_cooking` | 记录当时配置语义，避免以后配置变化改写历史 |
| `created_at` | 服务端 UTC 时间 |

个人汇总首版可以直接通过索引查询聚合，不必提前维护易失真的计数缓存。数据量增长后再增加物化汇总表。

建议索引：

- `visitors(token_hash)` 唯一索引；
- `draw_batches(visitor_id, created_at DESC)`；
- `draw_results(batch_id, position)`；
- 必要时增加便于双方和组合 TOP 查询的覆盖索引。

## 10. HTTP API 契约

统一前缀：`/api/v1`

统一成功响应直接返回资源对象；统一错误响应：

```json
{
  "error": {
    "code": "invalid_fixed_name",
    "message": "指定的派生不存在",
    "details": {}
  }
}
```

### 10.1 `POST /api/v1/session`

用途：创建或恢复匿名会话并设置 Cookie。

响应至少包含：

```json
{
  "visitor": {
    "created_at": "2026-09-18T00:00:00Z"
  }
}
```

不得把内部访客 ID、Cookie 原始令牌或令牌摘要返回给前端。

### 10.2 `GET /api/v1/catalog`

返回：

- 内容版本或 ETag；
- 彰人派生列表；
- 冬弥派生列表；
- 每个派生对应的同源头像 URL或缺图标记；
- 特殊结果的公开标签与文案；
- 前端需要展示的单次最大抽数。

不必向客户端暴露精确概率，除非页面明确准备展示概率说明。

### 10.3 `POST /api/v1/draw`

请求：

```json
{
  "count": 3,
  "fixed_side": "akito",
  "fixed_name": "黑百合"
}
```

规则：

- `count` 必须为 1–3；
- `fixed_side=none` 时 `fixed_name` 必须为空；
- 固定一方时名称必须来自对应当前派生池；
- 服务端完成随机和事务写入后才返回结果。

响应包含：

- 批次 ID 和服务端时间；
- 每个结果的双方名称、头像 URL、是否做饭、特殊类型和特殊文案；
- 本次抽取后的简要个人累计。

### 10.4 `GET /api/v1/me`

返回：

- 累计抽取次数；
- 做饭总次数；
- 每类特殊结果次数；
- 彰人派生 TOP 3；
- 冬弥派生 TOP 3；
- 组合 TOP 3；
- 最近 50 个单项结果，包含批次关系、抽取模式和时间。

### 10.5 `DELETE /api/v1/me/history`

- 前端必须先显示确认对话框。
- 删除当前匿名访客的批次和结果，并删除或重置访客记录。
- 成功后让当前浏览器获得新的匿名会话，避免旧 Cookie 指向不存在的数据。
- 操作不可恢复，页面需要明确提示。

### 10.6 `GET /healthz`

只返回服务是否健康、数据库是否可访问、内容快照是否有效。不得返回服务器绝对路径、环境变量或异常堆栈。

## 11. 前端体验

### 11.1 页面结构

建议使用底部导航或顶部标签切换三个主区域：

1. **抽派生**：首页和核心操作；
2. **派生池**：浏览与搜索；
3. **我的记录**：统计与最近记录。

### 11.2 抽取交互

- 默认“双方随机”和单抽。
- 固定一方时先选彰人或冬弥，再从可搜索选择器中选完整派生名。
- 抽取按钮在请求期间禁用，避免连点重复提交。
- 结果按 1–3 张卡片依次出现，可使用轻量 CSS 动画，但必须尊重 `prefers-reduced-motion`。
- 普通组合显示双方头像和 `彰人派生 × 冬弥派生`。
- 做饭结果使用明显但不过度闪烁的强调样式。
- 特殊结果按配置文案展示狐狸、兔子等素材；缺图时展示特殊结果名称和文字。
- 网络失败时保留当前结果并允许重试，不把失败请求计入历史。

### 11.3 派生池

- 彰人/冬弥分栏或标签切换。
- 支持按名称即时搜索。
- 卡片显示头像和名称。
- 图片使用懒加载；Nginx 对版本化静态素材设置长缓存。

### 11.4 我的记录

- 顶部显示累计抽数和做饭次数。
- 依次显示双方与组合 TOP 3。
- 最近记录最多加载 50 条，不做完整历史分页。
- 提供“清除我的记录”危险操作，二次确认后调用删除 API。

### 11.5 无障碍与适配

- 所有按钮和输入框有明确文本标签。
- 不只依赖橙色/蓝色区分双方，同时显示名称和角色标签。
- 图片带有可读替代文本。
- 键盘可以完成主要操作。
- 适配约 320px 起的窄屏，不以固定宽度图片作为主界面。

## 12. 安全与防滥用

虽然网站取消产品冷却，仍必须保护公开服务：

- 每个匿名会话每分钟最多 30 个抽取请求。
- 每个来源 IP 每分钟最多 60 个抽取请求。
- 一次请求最多 3 抽，因此单会话理论上每分钟最多产生 90 个结果。
- 超限返回 HTTP `429` 和 `Retry-After`。
- 首版单实例可使用内存令牌桶；如果以后多实例部署，迁移到 Redis 统一计数。
- 反向代理再设置连接数、请求体大小和基础速率限制，形成第二层保护。
- 只允许同源调用，不开启宽泛 CORS。
- 对 `POST`、`DELETE` 校验 `Origin`，请求体只接受 JSON。
- 配置 `TrustedHost` 或等效主机名校验。
- 添加 CSP、`X-Content-Type-Options: nosniff`、合理的 `Referrer-Policy` 和点击劫持防护。
- 所有 SQL 必须参数化。
- 静态素材通过内容清单映射，不允许用户输入直接拼接文件路径。
- 日志不记录完整 Cookie、令牌摘要、请求体中的敏感标识或数据库内容。
- API 异常返回稳定错误码，生产环境不返回 Python 堆栈。

## 13. 测试计划

### 13.1 领域单元测试

- 空输入默认为单抽、双方随机。
- 支持 1、2、3 抽，拒绝 0、4、负数、浮点数和字符串。
- 固定彰人和固定冬弥都能得到正确结果。
- 精确匹配优先于前缀和包含匹配。
- 模糊匹配歧义时返回候选错误。
- 固定名称不存在时拒绝请求。
- 确定性随机源可以复现普通、做饭和每类特殊结果。
- 特殊结果按权重选择。
- 同批次特殊标签正确去重。
- 特殊标签耗尽时回落普通结果。
- 空派生池和损坏配置返回明确领域错误。

### 13.2 统计测试

- 普通抽取累计双方和组合。
- 普通做饭同时累计组合和做饭。
- 特殊结果不累计双方或组合。
- `counts_as_cooking` 的特殊结果累计做饭。
- 三抽按三个结果累计。
- TOP 3 排序和同次数规则稳定。
- 最近记录只返回 50 项，顺序正确。

### 13.3 API 与数据库测试

- 首次访问设置安全 Cookie，后续访问恢复同一访客。
- 数据库不保存原始 Cookie 令牌。
- 无 Cookie 请求自动建立会话或按统一方式引导建立会话。
- 抽取成功同时写入批次和全部结果。
- 写入中途异常时完整回滚。
- 多个并发抽取请求不会丢数据。
- 非法参数、错误固定方、错误名称和超限均返回稳定错误结构。
- 清除记录只删除当前访客数据，不影响其他访客。
- 清除后旧会话不能读回已删除记录。
- 内容热重载失败时仍使用旧快照。
- 素材路径穿越请求被拒绝。
- `healthz` 在数据库不可用或内容无效时正确报告非健康状态。

### 13.4 前端验证

- 320px、375px、768px 和桌面宽度检查。
- 派生池搜索、分栏和缺图占位。
- 单抽和三抽结果布局。
- 做饭和各种特殊结果展示。
- 快速连点时只产生一个请求。
- 弱网、超时、429 和服务端错误提示。
- 刷新后个人记录仍存在。
- 清除记录的确认与完成状态。
- 键盘操作、焦点可见性和减少动画偏好。

## 14. 部署方案

### 14.1 现有环境

参考 Bot 文档，当前生产基线为阿里云 ECS，Bot 在独立 Docker 容器 `mybot` 中运行，宿主机仓库曾位于 `/akito_bot`。网站应新建自己的仓库目录、镜像、容器和持久化卷，不修改 `mybot` 的启动方式。

### 14.2 推荐拓扑

```text
互联网
  │  HTTPS 443
  ▼
Nginx / 证书
  │  宿主机回环地址
  ▼
paro-web Docker 容器
  ├─ 只读应用与 content
  └─ 可写 SQLite 数据卷

mybot Docker 容器（保持独立）
```

### 14.3 上线步骤

1. 为新项目建立独立 Git 仓库。
2. 在本地完成核心、API、前端和测试。
3. 购买或准备域名。
4. 因 ECS 位于中国大陆，完成 ICP 备案；备案完成前可使用本地或受限测试环境验证，不能把临时 IP 访问当作正式方案。
5. 配置域名 DNS 到 ECS。
6. 构建 `paro-web` Docker 镜像。
7. 创建独立 SQLite 持久化目录或 Docker volume，并限制权限。
8. 容器仅监听宿主机 `127.0.0.1` 的独立端口，不直接暴露公网。
9. Nginx 配置域名、反向代理、静态缓存、请求大小和限流。
10. 申请并自动续期 HTTPS 证书，HTTP 强制跳转 HTTPS。
11. 配置阿里云安全组，只开放必要的 80/443 和受控运维端口。
12. 执行健康检查、冒烟测试和备份恢复测试后开放访问。

### 14.4 备份与恢复

- 每日备份 SQLite 数据库，至少保留最近 7–14 天。
- 使用 SQLite 在线备份 API或在安全检查点后复制，不能在高并发写入时随意复制主文件。
- 备份文件不得放在 Web 可访问目录。
- 定期执行恢复演练，确认备份真正可用。
- 内容 JSON 和素材由 Git 保存，不与用户数据库备份混在一起。

### 14.5 可观测性

- 结构化访问日志：时间、路由、状态码、耗时、请求 ID；匿名访客只能记录不可逆的短期关联值，不能记录 Cookie。
- 应用指标：抽取成功/失败数、429 数、接口延迟、数据库忙错误、内容重载失败、缺图数量。
- Docker 健康检查调用 `/healthz`。
- 日志设置轮转和容量上限，避免占满 ECS 磁盘。

## 15. 隐私、版权与内容注意事项

### 15.1 隐私

网站虽然不要求注册，仍会使用 Cookie 并保存行为记录。页面应提供简短隐私说明，至少说明：

- Cookie 用于识别匿名访客和恢复个人记录；
- 保存抽取时间、抽取模式和结果；
- 不主动收集微信、QQ、手机号、邮箱等身份信息；
- 日志可能包含正常运维所需的 IP 信息，并按有限期限保留；
- 用户可自行清除个人记录；
- 联系方式和隐私说明应随正式域名一同公布。

### 15.2 版权

现有头像明显与《Project SEKAI》等角色衍生内容有关。**把项目从小程序改为自建网站只能避免微信平台审核，不能消除版权风险。**

在公开发布前必须：

- 确认头像、狐狸兔子素材、名称、Logo、字体和页面装饰的公开使用权；
- 保存授权或来源记录；
- 不使用会让用户误以为是官方产品的名称、域名、Logo 或宣传语；
- 在页面明确标注非官方粉丝项目，但要理解免责声明不能替代授权；
- 提供权利人联系和下架渠道；
- 技术上保持素材可替换：数据库只存逻辑名称，前端通过内容清单取图，换图不迁移历史记录。

若无法确认公开使用权，正式上线前应切换为纯文字色块或拥有版权的原创抽象图标。该项是发布条件，不是可由技术测试代替的事项。

## 16. 实施阶段

### 阶段一：项目骨架与内容迁移

- 初始化独立 Git 仓库、`pyproject.toml`、测试配置、Dockerfile 和环境变量示例。
- 建立推荐目录结构。
- 从 Bot 项目复制派生池、配置和素材到新项目 `content/`。
- 实现内容模型、校验、有效快照和缺图报告。
- 写清新项目与 Bot 的独立边界。

完成标准：内容可以在不导入 NoneBot 的 Python 进程中加载并通过测试。

### 阶段二：领域核心

- 迁移并整理抽取、定向匹配、特殊结果和统计规则。
- 使用明确的数据类或 Pydantic 模型代替无结构元组。
- 注入随机源，建立完整确定性测试。

完成标准：规则测试覆盖当前 Bot 行为，且模块不导入 FastAPI、SQLite 或 NoneBot。

### 阶段三：持久化与应用服务

- 建表和轻量迁移机制。
- 实现匿名访客仓储、抽取事务、个人聚合、最近 50 条和清除记录。
- 加入并发与事务测试。

完成标准：临时数据库中的数据在重启后仍可恢复，并发测试无丢失。

### 阶段四：API 与安全

- 实现会话、目录、抽取、个人记录、清除和健康检查接口。
- 实现统一错误、Origin 校验、安全 Cookie、限流和安全头。
- 增加 API 集成测试。

完成标准：全部接口契约稳定，非法输入和滥用请求不会破坏数据。

### 阶段五：响应式前端

- 完成抽取、派生池和我的记录三个区域。
- 实现结果卡、搜索、缺图、错误、加载和清除确认状态。
- 完成窄屏、桌面和无障碍验证。

完成标准：普通用户无需说明即可完成所有首版操作。

### 阶段六：容器化与内测

- 本地 Docker 构建与健康检查。
- 在 ECS 以独立容器部署到仅内网/回环端口。
- 使用临时受限入口进行功能、性能和恢复测试。
- 设置数据库备份、日志轮转和监控。

完成标准：容器重启、升级和数据库恢复均通过演练，不影响 `mybot`。

### 阶段七：正式上线

- 完成域名、ICP 备案、DNS、HTTPS 和 Nginx 配置。
- 完成素材权利确认和隐私说明。
- 执行生产冒烟测试后公开网址。

完成标准：HTTPS 正常、Cookie 安全、记录可持久化、限流有效、备份可恢复。

## 17. 明确不做的内容

为控制首版范围，以下内容不应在没有新需求时自行加入：

- 微信小程序和微信登录；
- QQ 登录或与 Bot 用户合并；
- 用户名、密码、邮箱和找回流程；
- 全站或每日排行榜；
- 用户投稿派生；
- 在线后台管理；
- 社交分享海报生成；
- 支付、广告或商业化；
- 多语言；
- 多实例部署、Redis、PostgreSQL；
- 将网站 API 挂入 NoneBot 进程。

## 18. 新对话接手提示

在 `C:\Users\Hasee\Desktop\paro_web` 开启新对话后，可以直接给开发代理如下指令：

> 请完整阅读 `IMPLEMENTATION_PLAN.md`，然后检查目录状态。以该文档为已确认需求，从“阶段一：项目骨架与内容迁移”开始实施。参考项目位于 `C:\Users\Hasee\Desktop\gemini_bot`，只能读取并参考其中的 `random_paro` 规则、配置、测试和素材，不要修改 Bot 仓库，也不要让新项目依赖 NoneBot。每完成一个阶段运行相应测试并汇报结果。

实施过程中，如果文档与实际 Bot 行为不一致：

1. 先通过读取上述参考文件和测试确认事实；
2. 仅对不影响产品范围的小差异作兼容处理；
3. 涉及概率、统计口径、公开功能、数据保留或版权素材的变化时，再向项目所有者确认；
4. 不要为了复用方便把代码写回 `gemini_bot`。

